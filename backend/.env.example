# Configuración del servidor
PORT=3000
NODE_ENV=development

# Configuración de la base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sgc_iso9001
DB_USER=postgres
DB_PASSWORD=tu_password_aqui

# JWT Configuration
JWT_SECRET=super_secret_jwt_key_change_in_production_min_32_chars_abcdef123456789
JWT_EXPIRES_IN=24h

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Configuración de archivos (opcional)
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# Configuración de email SMTP (opcional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_email@gmail.com
SMTP_PASSWORD=tu_app_password
EMAIL_FROM=noreply@sgc.com

# Configuración de logs (opcional)
LOG_LEVEL=debug

# Configuración de sesiones (opcional)
SESSION_SECRET=tu_session_secret_aqui

# URLs del frontend (para cors y redirects)
FRONTEND_URL=http://localhost:5173
ADMIN_URL=http://localhost:5173/admin

# Configuración de certificados SSL (producción)
SSL_CERT_PATH=
SSL_KEY_PATH=

# API Keys externas (opcional)
EXTERNAL_API_KEY=
NOTIFICATION_SERVICE_KEY=

# Configuración de backup (opcional)
BACKUP_SCHEDULE=0 2 * * *
BACKUP_PATH=./backups

# Límites de rate limiting (opcional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
